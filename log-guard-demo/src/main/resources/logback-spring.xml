<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Boot's console pattern prints neither the MDC nor key-value pairs, and both are masked.
         Each is dropped when empty, which on most lines it is. -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{28} %replace(%replace(mdc=%mdc ){'traceId=[0-9a-f]+, spanId=[0-9a-f]+(, )?', ''}){'mdc= ', ''}%replace(kvp=%kvp ){'kvp= ', ''}- %msg%n%throwable</pattern>
        </encoder>
    </appender>

    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OTEL"/>
    </root>
</configuration>
